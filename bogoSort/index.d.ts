/**
 * Sorts array with bogosort
 * @param {Array.<number>} array array to sort
 * @returns {Array.<number>} sorted array
 */
declare const bogoSort: (array: number[]) => number[];
export default bogoSort;
