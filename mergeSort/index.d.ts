/**
 * Main mergesort algorithm
 * @param {Array.<number>} array - array to sort
 * @returns {Array.<number>} sorted array; does not sort in-place
 */
declare const mergeSort: (array: number[]) => number[];
export default mergeSort;
