#ifndef UTILS_H
#define UTILS_H
#include <vector>

namespace util{
    std::vector<int> randomSequence(int minimum, int maximum);
    void printArray(const std::vector<int> &array);
}//namespace util

#endif //UTILS_H
